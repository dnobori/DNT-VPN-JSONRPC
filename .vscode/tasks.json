{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/DNT-VPN-JSONRPC/DNT-VPN-JSONRPC.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}